INSTRUCCIONES

(TERMINAL1)$ sh pruebas.sh 
	('Inserta parametro: 1 borrar, 0 añadir')
	0

(TERMINAL2)$ sh pruebas.sh
	1

(TERMINAL3)$ hacer /cat/proc/modlist cada segundo y ver que la lista no crece en más de uno, y a la que se añade se borra (por
el retraso de lanzamiento)
